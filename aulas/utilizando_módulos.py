# se tu quiser colocar uma biblioteca usa-se import para carregar uma biblioteca no programa permitindo o uso de comandos que não tem no python normalmente
import math
# permitindo o uso de novos comandos que não tem no python normalmente como, pow (potencialzação), ceil (arredondar pra cima), sqrt (raiz quadrada) e etc...

# se quiser puxar apenas uma função expecifica de uma biblioteca use
from math import pow, sqrt
# isso ira fazer apenas a(s) função(s) funcionar no programa sem puxar a biblioteca toda